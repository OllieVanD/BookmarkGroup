// require('dotenv').config()
// const { models } = require('../models'); 
//     const createEntry = async (testInput = "www.testUrl.com") => {
//         console.log("entering data")
//         await models.bookmarks.create({ url : testInput})
//   }

// module.exports = createEntry