<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bookmarks</title>
</head>
<body>
    <header><h1>Welcome to your page of bookmarks!</h1></header>
    <div> <hr>
        <h2> <ul>
            <% bookmarks.map((bookmarks,index) =>{  %>
                <li id ='bookmarks'><%=`${bookmarks.url}` %> </li>
                <form method ="post" action="/bookmarks/<%=bookmarks.id%>?_method=DELETE">
                <button type="submit" id="<%= index %>" >Delete</button>
            </form>
            <form method ="get" action="/update/<%=bookmarks.id%>?_method=UPDATE">
            
                <button type="submit" id="<%= index %> " >Update</button>
            </form>

               <% }) %> 
            </ul>
        </h2>
        <hr>
        <h2>Enter your new bookmark!</h2>
            <form method="post" action="/addUrl">
                <input name = "bookmarkUrl" placeholder="Enter URL" id="inputBookmark">
                <button name = 'submit' id="submit">Submit</button>
            </form>

        
    </div>

</body>
</html>